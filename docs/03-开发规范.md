# 开发规范（强约束）

## 核心原则
必须严格按照docs开发，不允许自由发挥。

## 数据库规则
- 不允许新增字段
- 修改必须先改文档

## API规则
统一返回：
{
  "code": 0,
  "message": "",
  "data": {}
}

REST规范：
GET / POST / PUT / DELETE

## AI规则
AI只负责解析，不做决策

## 页面规则
- 页面必须对应docs
- 不允许自由新增页面

## 强制规则
禁止：
- 自由设计业务
- 跳过文档
- 临时改结构